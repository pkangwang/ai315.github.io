import{_ as s,c as p,o as a,a2 as e}from"./chunks/framework.84TDCN6B.js";const v=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"Python/pip：Python包管理器.md","filePath":"Python/pip：Python包管理器.md"}'),t={name:"Python/pip：Python包管理器.md"},i=e(`<p>pip是Python官方的包管理器，默认随着Python程序一起安装。</p><p>要检查pip是否已经安装，在终端中运行：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>pip</span></span></code></pre></div><p>如果已经安装，则会输出pip的帮助信息。</p><p><strong>安装包</strong></p><p>要安装包，运行如下命令：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>pip install package</span></span></code></pre></div><p>pip默认会将包安装到如下位置：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>Python安装目录\\Lib\\site-packages</span></span></code></pre></div><p><strong>显示已安装的包列表</strong></p><p>要显示已经安装的包列表，运行如下命令：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>pip list</span></span></code></pre></div><p><strong>卸载包</strong></p><p>要卸载某个包，运行如下命令：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>pip uninstall package -y</span></span></code></pre></div><p>加上 -y 或 --yes 选项表示确认卸载，如果不加，则需要在终端中手动确认。</p><p><strong>切换下载源</strong></p><p>可以将包的下载源地址切换到国内源，例如阿里云的源，以提升下载速度。有两种方式：命令替换和手动替换。</p><p>第一种方式，命令替换，推荐使用。运行如下命令，切换成阿里云的源：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>pip config set global.index-url https://mirrors.aliyun.com/pypi/simple</span></span></code></pre></div><p>第二种方式，手动替换。在用户主目录下，例如Windows中的<code>C:\\Users\\username\\</code>下，创建文件<code>pip.ini</code>，并添加如下内容：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>[global] </span></span>
<span class="line"><span>index-url = https://mirrors.aliyun.com/pypi/simple/</span></span></code></pre></div><p>这样，pip会默认使用这个源。</p>`,23),n=[i];function o(l,c,d,h,r,g){return a(),p("div",null,n)}const _=s(t,[["render",o]]);export{v as __pageData,_ as default};
