import{_ as a,c as s,o as e,a2 as t}from"./chunks/framework.84TDCN6B.js";const _=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"Linux Shell/列字段筛选工具——awk命令.md","filePath":"Linux Shell/列字段筛选工具——awk命令.md"}'),i={name:"Linux Shell/列字段筛选工具——awk命令.md"},p=t('<p>语法</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>awk  选项  &#39;行筛选条件{列编辑指令}&#39; 文件或列表</span></span></code></pre></div><p>选项： -F 指定分隔符</p><p>示例：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>cat /etc/passwd | awk -F &quot;:&#39; &quot;{print $1,$3,$4}&#39;&#39;</span></span></code></pre></div><p>以&quot;:&quot;为分隔符，输出1，3，4列内容</p><h2 id="内部变量" tabindex="-1">内部变量 <a class="header-anchor" href="#内部变量" aria-label="Permalink to &quot;内部变量&quot;">​</a></h2><ul><li>FS：指定每行文本的字段分隔符，默认为空格或制表位。 与-F一样</li><li>NF：当前处理的行的字段个数。</li><li>NR：当前处理的行的行号（序数）。</li><li>$0：当前处理的行的整行内容。</li><li>$n：当前处理行的第 n 个字段（第 n 列）。</li><li>FILENAME：被处理的文件名。</li><li>RS：数据记录分隔，默认为\\n，即每行为一条记录。</li></ul><h2 id="行筛选条件" tabindex="-1">行筛选条件 <a class="header-anchor" href="#行筛选条件" aria-label="Permalink to &quot;行筛选条件&quot;">​</a></h2><p>输出奇数行：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>nl /etc/passwd | awk &#39;(NR%2)==1{print}&#39;</span></span></code></pre></div><p>输出1、3行的内容：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>cat /etc/passwd | awk &#39;NR==1||NR==3{print}&#39;</span></span></code></pre></div><h2 id="if语句" tabindex="-1">if语句 <a class="header-anchor" href="#if语句" aria-label="Permalink to &quot;if语句&quot;">​</a></h2><p>必须用在{}中，且比较内容用()扩起来</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>awk -F: &#39;{if($1~/mail/) print $1}&#39; /etc/passwd                                       //简写</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>awk -F: &#39;{if($1~/mail/) {print $1}}&#39;  /etc/passwd                                   //全写</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>awk -F: &#39;{if($1~/mail/) {print $1} else {print $2}}&#39; /etc/passwd            //if...else...</span></span></code></pre></div>',18),l=[p];function n(c,o,d,h,r,u){return e(),s("div",null,l)}const k=a(i,[["render",n]]);export{_ as __pageData,k as default};
